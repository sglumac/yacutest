image:
  - Ubuntu

build: off

install:
- pip3 install gcovr

test_script:
- mkdir build
- cd build
- cmake -DCOVERAGE=True -DCMAKE_BUILD_TYPE=Debug ..
- cmake --build . --target yacucoverage

after_test:
- gcovr -r . --xml coverage.xml
- codecov
